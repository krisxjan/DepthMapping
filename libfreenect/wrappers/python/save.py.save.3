import freenect
import cv2
import frame_convert2

print('Press ESC in window to save depth and press space to save video')

def get_depth():
    return frame_convert2.pretty_depth_cv(freenect.sync_get_depth()[0])


def get_video():
    return frame_convert2.video_cv(freenect.sync_get_video()[0])


while(1):
    
    print(k)
    if k == 32:
        print(k+1)
        file = 'depth/newDepth.jpg'
        cv2.imwrite(file, get_depth())
        file2 = 'video/newVideo.jpg'
        cv2.imwrite(file2, get_video())
        break
